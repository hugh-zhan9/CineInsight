# Jellyfin 首期兼容设计

## 一、修订历史

V1.0.0 · 2026-09-07 · Codex：根据用户确认范围建立接口、状态和安全边界。

V1.0.1 · 2026-09-07 · Codex：独立评审后明确不在 HTTP 中补字幕索引、播放能力校验的支持范围、GORM 凭据脱敏与停服 drain。

## 二、需求信息

来源：[requirements.md](../../../../.loopx/intake/2026-09-07-jellyfin/requirements.md)，AC-01..05 / TC-01..05。只承诺被验收的客户端流程；首期不宣称完整 Jellyfin 兼容。

## 三、概要设计

### 3.1 方案

架构、替代方案和所有权见 [概要设计](概要设计.md)。

### 3.6 架构检查

`api-designer`：外部 DTO 用 Jellyfin PascalCase，内部模型不改命名；外部 offset 分页不强行转换为片库游标。`architecture-designer`：复用 `services/library_service.go:applyLibraryFilter` 与 `SetVideoFavorite/UpdateVideoWatchProgress/SetVideoWatched`；`go-style`：沿用 services 同包分文件与 App 生命周期接线。

## 四、详细设计

### 4.1 D-01：网络与身份（AC-01）

Operational / Interface Contract：监听独立 TCP 端口，默认 8096，默认关。仅接受回环/内网/链路本地地址，不信任转发头。除公开服务器信息和登录外，每条路由包括图片、字幕及 Range 都要 token。Origin 存在时校验同源。只提供一个非管理员账户，不能删除媒体或改全局配置。

配置由独立 Wails 方法保存。通用 SettingsService.UpdateSettings 的整行 Save 必须显式 Omit 全部 Jellyfin 列，避免旧快照覆盖独立配置；独立配置 owner 串行化读取、数据库字段更新、旧连接关闭/令牌失效与按新配置启动，两个配置调用不得交错。数据库写失败保持原实例；写成功但监听失败保留新配置并显示未运行/失败，可修正端口后重试。密码 8–72 UTF-8 字节，bcrypt 哈希；用户名非空且 ≤64 字符。密码留空表示保持；首次开启必须已设置密码。server ID 首次配置时随机生成并持久化。公开状态不含 hash、明文密码、token；日志不得记录凭据或媒体路径；包含 hash 的配置 UPDATE 用局部静默 ORM logger，避免失败 SQL 插值泄露。

随机 token 256 bit，服务端只存 SHA-256 摘要；最多 64 个登录会话，每个有效期 30 天，达到上限拒绝新登录；退出登录清除该会话，关闭/更改配置/重启全部失效。登录限制并发哈希及短窗口尝试次数，失败不泄露账号存在性。请求体读取期限 15 秒，响应不设统一写超时以允许长片播放。停服关闭新请求准入、取消活动连接并等待全部 handler 退出，应用恢复数据库前先停服。

### 4.2 D-02：片库与 DTO（AC-02）

Behavior Contract：顶层提供全部视频、收藏、继续观看、未看、已看、最近添加、最近播放、标签、作品集和每个保存视图。实体用带类型前缀的 32 位十六进制 ID，避免相同数字 ID 跨表碰撞。公开媒体必须是当前扫描范围内的活跃且非失效视频；保存的失效视图不开放失效文件。返回名字、标签、个人评分、时长、现有流快照、观看状态，不返回本机绝对路径。

查询复用 `applyLibraryFilter`；没有扫描根时返回空库。字幕条件只查已有索引，HTTP 不触发全库索引同步；在同一查询追加外部筛选，不覆盖保存视图原筛选。默认每页 100、上限 200，支持 StartIndex/Limit、白名单排序、SearchTerm、标签、收藏/已看筛选；空页 Items 为 []。不认识的排序/影响筛选的值明确报错，不能拼进 SQL。图片和未知资源类型不能回落为视频。

### 4.3 D-03：原片播放与状态（AC-03）

Interface / Data Contract：PlaybackInfo 校验 direct-play 容器/编解码器、码率上限及可判断的 profile 条件（宽高、视频 profile、码率/音频参数/帧率）。未知条件、ApplyConditions 或 SubContainer 约束不能证明可播放时返回 NoCompatibleStream；query 的 EnableDirectPlay/MaxStreamingBitrate 同样生效。只声明 direct play，不给转码 URL；视频经 ID 解析原文件，GET/HEAD/Range 复用 ServeContent，不接受客户端路径。缺失流快照可在明确播放请求内调用已有 MediaProbeService.Refresh；失败明确返回不可播放。外置同名 SRT 可由受认证 ID 路由读取。封面复用 ThumbnailService。

Sessions/Playing、Progress、Stopped 写 `UpdateVideoWatchProgress`；到片尾才标记已看；收藏与已看动作分别复用现有 setter。进度秒=ticks/10^7，负数拒绝，超过时长由原服务裁剪。重复回报覆盖同一状态，不新增播放账本事件（现有来源闭集不扩展）。兼容入口写入串行化，保持到达顺序；多设备同一视频沿用现有最后到达写入语义，允许向后 seek，不按时间最大值合并。与桌面并发仍沿用服务的字段级更新，收藏不覆盖进度。

### 4.4 D-04：手机网页（AC-05）

Behavior Contract：FeedSheet Teleport 到 body，跟随 visualViewport 的 height/offsetTop/offsetLeft/width，监听 resize/scroll 且卸载清理；没有该 API 时用原固定满屏。内容区独立滚动，输入框/页脚不被 flex 压扁。标签多行、长名可换行，最多占可见高度约三成，超出可滚动；触摸/滚轮/键盘不转为切视频。

所有模块边界：空输入、缺记录、越界资源直接报错；不自动安装依赖、不新增后台重试。桌面播放、Feed API 和其反馈投影保持原语义。

## 五、存储

D-01 配置在现有 Settings 增加字段，由已有 ApplySchema 自动迁移；哈希 json:"-"，无新业务表。会话与限流 map 有界且随停服释放。无数据库应用锁、无新增第二套视频状态。

## 七、接口

公开：GET System/Info/Public、POST Users/AuthenticateByName。
认证：System/Info、Users/Me、Users/{id}、Users/{id}/Views、Items 与 Users/{id}/Items、单项详情、Items/{id}/Images/Primary、Items/{id}/PlaybackInfo、Videos/{id}/stream、外置字幕、Sessions/Playing[/Progress|/Stopped]、用户 FavoriteItems/PlayedItems、Sessions/Logout。

支持标准 Authorization MediaBrowser Token、X-Emby-Token、媒体 URL api_key/ApiKey；所有入口统一校验。具体字段以官方 Jellyfin API 与客户端证据为准，不盲目模拟管理接口。

## 八、发布

新增默认关闭。设置页明确显示仅视频、无转码、需桌面运行、重启需重新登录。出问题关闭入口即可；不迁移或改写既有媒体。

## 九、维护

状态显示监听地址和启动错误；不会因端口占用拖垮主程序。客户端请求失败只记录路由类型和状态，不记录完整 URL/查询/token/媒体路径。

## 10.5 Planning Handoff

按网络认证、片库播放、设置整合与手机修复验收分片；先独立评审计划，安全/API 改动在交付前独立评审精确 diff。Fileball 真机证据属于交付验收，不可用 mock 替代。

## 十一、QA

11.2 验证：TC-01/02/03 用 httptest + SQLite fixture，认证/并发用 race；TC-05 用 Vue 组件视口事件回归及布局检查；全量 go test ./...、go vet ./...、npm test、npm run build。TC-04 必须真机记录 Fileball 版本与请求/结果；没有连接到真机时明确待验收。

11.3 门禁：不能从 API 测试通过推出 Fileball 已兼容。11.4 契约索引：D-01→AC-01/TC-01，D-02→AC-02/TC-02，D-03→AC-03/TC-03，D-04→AC-05/TC-05；AC-04/TC-04→最终真机验收。

官方依据（2026-09-07 核对）：[Jellyfin 登录](https://kotlin-sdk.jellyfin.org/guide/authentication.html)、[官方播放器 API 笔记](https://github.com/jellyfin/jellyfin-mpv-shim/blob/master/docs/jellyfin-api-notes.md)、[Fileball 更新记录](https://fileball.app/changelog/)。Fileball 的具体调用组合尚未取得真机证据。
